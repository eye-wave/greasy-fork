{
  "name": "@packages/picew-tools",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "author": {
    "name": "eye-wave",
    "url": "https://github.com/eye-wave"
  },
  "scripts": {
    "dev": "builder --watch --web --open",
    "format": "biome format . --write",
    "lint": "biome lint . --apply-unsafe",
    "test": "bun test --coverage",
    "build": "builder -o dist/picrew-tools.user.js"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.7.1",
    "@scripts/build": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "license": "GPL-3.0+",
  "dependencies": {
    "vanjs-core": "^1.5.0",
    "@repo/utils": "workspace:*"
  }
}

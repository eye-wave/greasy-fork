{
  "name": "greasyfork-scripts",
  "description": "collection of my user scripts for greasyfork",
  "private": true,
  "scripts": {
    "format": "turbo run format",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "turbo run build",
    "clean": "find . -type d -name 'dist' -exec rm -r {} \\; > /dev/null 2>&1"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ],
  "devDependencies": {
    "turbo": "^1.13.2"
  },
  "workspaces": [
    "packages/*",
    "scripts/*"
  ],
  "license": "GPL-3.0+"
}
